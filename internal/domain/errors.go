package domain

const (
	ErrEmailIsRequired    = "email is required"
	ErrIdIsRequired       = "id is required"
	ErrUsernameIsRequired = "username is required"
	ErrUsernameIsNotValid = "username is not a valid"
	ErrPasswordIsRequired = "password is required"
	ErrAppIdIsRequired    = "app id is required"
	ErrPasswordIsNotValid = "password is not valid. try to change the password and try again"
	ErrUserNotFound       = "user not found"
	ErrWrongPassword      = "wrong password"
	ErrAppNotFound        = "app not found"
	ErrSomethingWentWrong = "something went wrong. we're working on it"
)
